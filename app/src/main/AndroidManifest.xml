<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.opticscompanion">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name=".Activities.CenteringActivity_Nav"></activity>
    <activity android:name=".Activities.ZygoActivity" />
    <activity android:name=".Activities.ZygoActivity_Nav" />
    <activity android:name=".Activities.ProcessActivity" />
    <activity android:name=".Activities.ProcessActivity_Nav" />
    <activity android:name=".Activities.LensActivity_Nav">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".Activities.LensActivity" />
    <activity android:name=".Activities.MainActivity_Nav"></activity>
  </application>

</manifest>